# -------------------------------------------------------------------
# Super QA AI Assistant - Environment Configuration
# -------------------------------------------------------------------
# Instructions:
# 1. Copy this file to a new file named .env in the root directory.
# 2. Fill in the required API keys and model configurations below.
# 3. DO NOT commit the .env file to version control.

# --- AI Provider Configuration ---

# Set the primary AI provider to use for generation.
# Options: "openai" or "anthropic"
# Default: "openai"
PRIMARY_PROVIDER=openai

# --- OpenAI Configuration ---

# Your OpenAI API key.
# Required if PRIMARY_PROVIDER is "openai" or as a fallback.
OPENAI_API_KEY="YOUR_OPENAI_API_KEY_HERE"

# The OpenAI model to use. `gpt-4o-mini` is recommended for a balance of speed and quality.
# Other options: `gpt-4o` (more powerful, slower, more expensive)
OPENAI_MODEL=gpt-4o-mini

# Timeout for OpenAI API requests in milliseconds.
# Default: 60000 (60 seconds)
OPENAI_TIMEOUT=60000

# --- Anthropic Configuration ---

# Your Anthropic API key.
# Required if PRIMARY_PROVIDER is "anthropic" or as a fallback.
ANTHROPIC_API_KEY="YOUR_ANTHROPIC_API_KEY_HERE"

# The Anthropic model to use. `claude-3-5-haiku-20240620` is recommended for speed.
# Other options: `claude-3-sonnet-20240229` (more powerful, slower)
ANTHROPIC_MODEL=claude-3-5-haiku-20240620

# Timeout for Anthropic API requests in milliseconds.
# Default: 60000 (60 seconds)
ANTHROPIC_TIMEOUT=60000

# --- AI Monitoring & Analytics (Optional) ---

# Your Helicone API key for monitoring and analytics.
# This is optional but highly recommended for production and debugging.
# Get a key from https://www.helicone.ai/
HELICONE_API_KEY="YOUR_HELICONE_API_KEY_HERE"
